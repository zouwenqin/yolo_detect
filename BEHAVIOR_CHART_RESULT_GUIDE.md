# 行为统计图表结果展示说明

本文档用于比赛展示时讲解“行为统计”页面中的核心图表结果。展示口径以“录制课堂监控视频素材分析”为主线，强调系统对课堂视频中人物行为的识别、统计、预警与教师辅助干预，不把结果表述为医学诊断、心理诊断或学生实名评价。

## 1. 图表展示的整体作用

行为统计页面的目标不是单纯展示检测框，而是把算法识别到的逐帧结果转换成教师和评委容易理解的课堂状态信息。

核心链路如下：

```text
课堂视频素材
  -> YOLOv8 识别人物行为
  -> 行为标签归一化
  -> 按时间累计人次时长
  -> 生成趋势、占比和空间分布
  -> 辅助判断课堂状态和异常行为风险
```

页面中的几个图表分别回答四类问题：

| 图表 | 主要回答的问题 | 展示价值 |
| --- | --- | --- |
| 累计人次时长 | 哪些行为出现得最多、持续得最久 | 量化整段课堂视频中的行为强度 |
| 人次时长趋势 | 行为在什么时间段上升或下降 | 发现异常行为集中出现的时间窗口 |
| 累计行为占比 | 不同行为在整体课堂状态中占多少 | 快速判断课堂整体秩序和专注程度 |
| 行为空间热力图 | 异常行为主要集中在画面哪些区域 | 辅助教师定位课堂区域和回看视频片段 |

## 2. 累计人次时长

### 2.1 含义

累计人次时长表示某一类行为在整段视频中被检测到的总持续量，单位可以理解为“人次秒”或换算后的“分秒”。

它不是简单的视频播放时长，而是把“人数”和“持续时间”结合起来统计。例如：

```text
1 名学生持续低头 10 秒 = 10 人次秒
3 名学生同时低头 10 秒 = 30 人次秒
```

因此，累计人次时长能够体现某类行为在课堂中的总体影响程度。截图中的示例包括：

- 低头：3 分 46 秒，累计占比 37.9%，片段 38 段，置信度中。
- 趴桌：2 分 41 秒，累计占比 27.1%，片段 28 段，置信度高。
- 专注/正常：1 分 54 秒，累计占比 19.2%，片段 19 段，置信度高。
- 其他行为：1 分 34 秒，累计占比 15.8%，片段 15 段，置信度中。

### 2.2 统计维度说明

每张统计卡片通常包含以下信息：

| 字段 | 说明 |
| --- | --- |
| 行为名称 | 系统归一化后的行为类别，如低头、趴桌、专注/正常、其他行为 |
| 累计时长 | 该行为在整段视频中的累计人次时长 |
| 累计占比 | 该行为时长在所有行为累计时长中的比例 |
| 片段数 | 行为被检测成连续片段的次数 |
| 置信度 | 算法对该类行为识别结果的整体可信程度 |

### 2.3 作用

累计人次时长主要用于量化课堂行为强度。它能帮助评委看到系统并不是只做单帧识别，而是把连续检测结果进一步整理成可分析的数据。

对于教师来说，该指标可以辅助判断：

- 哪类异常行为最突出。
- 某类行为是偶发出现，还是持续存在。
- 异常行为是否已经达到需要关注的程度。
- 后续 AI 辅助干预建议应优先围绕哪类行为展开。

### 2.4 比赛讲解话术

可以这样讲：

> 这里的累计人次时长不是普通的视频时长，而是把“检测到的人数”和“持续时间”结合起来统计。比如 3 名学生同时趴桌 10 秒，系统会统计为 30 人次秒。这样比单纯计数更能反映课堂异常行为的实际影响范围。

## 3. 人次时长趋势

### 3.1 含义

人次时长趋势图展示不同行为随视频时间变化的累计强度。横轴表示视频时间点或时间片段，纵轴表示对应时间窗口内的行为人次时长。

截图中的趋势图包含多条曲线，例如：

- 持续低头
- 趴桌
- 专注学习
- 其他行为

每条曲线代表一种行为在视频播放过程中的变化情况。曲线上升表示该行为在该时间段变得更明显，曲线下降表示该行为减少或趋于缓和。

### 3.2 作用

人次时长趋势图主要用于发现“什么时候发生了变化”。

它可以帮助展示以下能力：

- 从静态统计升级为时间序列分析。
- 观察异常行为是否集中在某几个时间段。
- 判断课堂状态是否随着教学过程发生变化。
- 为教师回看视频提供时间定位依据。

例如，如果“趴桌”曲线在 00:10 之后明显升高，说明视频后半段可能出现注意力下降或疲劳迹象。教师可以结合课堂内容回看该时间段，判断是否与讲解节奏、课堂互动减少、作业环节等因素有关。

### 3.3 与累计人次时长的区别

| 指标 | 关注点 | 适合回答 |
| --- | --- | --- |
| 累计人次时长 | 整段视频总量 | 哪类行为最突出 |
| 人次时长趋势 | 时间变化过程 | 异常行为什么时候出现或增强 |

累计人次时长看的是“总结果”，趋势图看的是“过程变化”。两者结合后，既能说明整体课堂状态，也能定位关键时间段。

### 3.4 比赛讲解话术

可以这样讲：

> 趋势图体现的是行为随时间变化的过程。系统不仅知道整段视频里低头、趴桌分别持续了多久，还能定位这些行为在哪些时间段集中出现。这样教师后续回看视频时，不需要从头到尾人工查找，可以直接关注异常曲线升高的时间窗口。

## 4. 累计行为占比

### 4.1 含义

累计行为占比图通常以环形图或饼图展示，用来表示各类行为在全部累计人次时长中的比例。

它的计算逻辑可以理解为：

```text
某行为累计占比 = 某行为累计人次时长 / 所有行为累计人次时长
```

截图中的示例显示，低头、趴桌、专注/正常、其他行为分别占据不同颜色区域，教师可以快速看到整段课堂视频中哪类行为占主导。

### 4.2 作用

累计行为占比主要用于快速判断课堂整体状态。

它适合在比赛展示中承担“概览图”的作用：

- 让评委快速理解整段视频的行为结构。
- 对比正常行为和异常行为的比例。
- 判断课堂整体风险是否偏高。
- 为后续预警事件和 AI 建议提供数据依据。

如果异常行为占比较高，说明这段课堂视频中需要关注的行为较多；如果专注/正常占比较高，说明整体课堂状态相对稳定。

### 4.3 讲解时的注意点

占比图不能单独作为最终判断依据。因为占比高可能来自多个学生短时间集中出现，也可能来自少数学生长时间持续出现。因此，比赛展示时建议把占比图与累计时长、趋势图和空间热力图一起解释。

### 4.4 比赛讲解话术

可以这样讲：

> 环形图展示的是整段视频中各类行为的组成比例。它能让教师快速判断课堂整体状态，例如异常行为占比较高时，说明这段课堂需要进一步关注；但系统不会只凭一个占比下结论，还会结合持续时长、时间趋势和空间分布生成更稳妥的预警结果。

## 5. 行为空间热力图

### 5.1 含义

行为空间热力图是在课堂视频画面上叠加行为统计结果，用来展示异常行为在画面空间中的分布情况。

系统会根据检测框、头部或肩背等关键区域的位置信息，统计某个画面区域中异常行为的累计时长。图中的标记通常表示：

| 可视化元素 | 含义 |
| --- | --- |
| 红色标记 | 高风险或持续较久的异常行为 |
| 紫色标记 | 累计越深表示持续越久 |
| 绿色提示 | 正常或短暂行为 |
| 标记中的秒数 | 该位置对应行为的累计持续时间 |
| 右侧 Top 列表 | 异常行为持续时间较长的若干检测对象或区域 |

截图中右侧列表展示了异常行为 Top 5，例如 ID 20、ID 18、ID 10、ID 4、ID 13 的趴桌行为持续时间较长，风险等级为高。

### 5.2 作用

行为空间热力图主要用于回答“异常行为集中在哪里”。

它有三方面展示价值：

1. 空间定位：帮助教师快速看到异常行为集中在教室前排、后排、靠窗、角落等区域。
2. 视频回看：教师可以根据热力标记回看对应画面位置和时间片段。
3. 课堂管理分析：如果异常行为集中在某个区域，可能与座位位置、视线遮挡、课堂互动不足等因素有关。

### 5.3 与学生身份的关系

比赛展示时要特别注意表达方式。热力图中的 ID 是算法检测过程中的临时目标编号，用于视频分析和事件定位，不应表述为学生实名追踪。

建议使用以下表达：

- “检测对象编号”
- “画面区域”
- “异常行为位置”
- “课堂事件定位”

避免使用以下表达：

- “锁定某个学生”
- “给学生贴标签”
- “持续监控个人”
- “诊断学生状态”

### 5.4 比赛讲解话术

可以这样讲：

> 空间热力图把行为结果叠加到课堂画面上，帮助教师看到异常行为主要集中在哪些区域。这里的 ID 是检测过程中的临时编号，不对应学生实名。系统的目标是辅助教师定位课堂事件和回看视频片段，而不是对学生进行公开点名或标签化评价。

## 6. 四类图表之间的逻辑关系

比赛展示时，建议按照“总量 -> 趋势 -> 占比 -> 空间”的顺序讲解。

```text
累计人次时长：说明异常行为总量有多少
人次时长趋势：说明异常行为什么时候集中出现
累计行为占比：说明异常行为在整体课堂状态中占多大比例
行为空间热力图：说明异常行为集中在画面哪些区域
```

四类图表共同构成一个完整解释链：

```text
识别到了什么行为
  -> 这些行为持续了多久
  -> 这些行为何时出现
  -> 这些行为占比多少
  -> 这些行为集中在哪里
  -> 是否需要生成预警和 AI 辅助干预建议
```

这样可以体现项目从“算法识别”到“课堂辅助决策”的闭环能力。

## 7. 答辩时可强调的创新点

### 7.1 从单帧识别到时序统计

系统不是只判断某一帧中是否出现低头或趴桌，而是持续统计行为时间。这样可以减少偶然动作带来的误判，更适合课堂场景。

### 7.2 从检测结果到课堂事件

检测框本身只是一种技术结果，教师真正需要的是可理解、可处理的课堂事件。图表把底层检测数据转成了行为时长、趋势、占比和空间分布，降低了理解成本。

### 7.3 从异常发现到辅助干预

图表结果可以继续进入 AI 辅助干预和报告页面，生成非诊断式沟通建议。系统强调辅助教师发现问题和组织沟通，而不是替代教师判断。

## 8. 展示时的推荐讲解顺序

1. 先说明输入来源：系统分析的是录制好的课堂监控视频素材。
2. 展示累计人次时长：说明系统能够量化不同行为的持续程度。
3. 展示趋势图：说明系统能够定位异常行为出现的时间段。
4. 展示占比图：说明系统能够概览课堂整体行为结构。
5. 展示空间热力图：说明系统能够定位异常行为集中区域。
6. 进入预警事件或 AI 干预建议：说明图表结果如何服务后续教师处理。

## 9. 一段完整展示话术

可以在比赛现场这样串讲：

> 这一页展示的是行为统计结果。系统对录制课堂视频逐帧分析后，不只是输出检测框，而是把识别结果转换成教师更容易理解的统计图表。上方的累计人次时长表示某类行为在整段视频中的总持续量，它把人数和时间结合起来，例如多人同时趴桌会累计为更高的人次时长。中间的趋势图展示各类行为随时间的变化，帮助教师定位异常行为集中出现的时间段。右侧的累计行为占比用于快速判断整段课堂中正常行为和异常行为的比例。下方的空间热力图把异常行为叠加到课堂画面上，帮助定位行为集中区域。通过这些图表，系统完成了从视频识别、行为统计、风险预警到 AI 辅助干预的展示闭环。

## 10. 风险与隐私表述建议

比赛展示中建议强调：

- 结果用于课堂状态辅助分析，不替代教师判断。
- AI 建议是非诊断式沟通建议，不是医学或心理诊断。
- 检测 ID 是算法临时编号，不等同于学生实名身份。
- 系统重点是发现课堂事件和辅助干预，不是公开点名或长期追踪个人。
- 图表结果需要结合教师现场经验、课堂内容和视频上下文综合理解。

