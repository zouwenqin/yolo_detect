# 江西省职业技能大赛人工智能赛道展示执行手册

适用场景：大专组人工智能赛道“三自主”项目展示，预计展示时长 50 分钟，团队 4 人。

项目定位：课堂行为检测与 AI 辅助干预系统。系统基于 Vue 前端、Spring Boot 后端、Flask + YOLOv8 算法服务和 MySQL 数据库，实现“视频/摄像头输入 -> 行为识别 -> 风险事件 -> AI 干预建议 -> 报告导出”的完整闭环。

## 1. 总体展示策略

本项目展示不建议现场手写大段代码。比赛重点不是临时写代码，而是证明团队能够完成一个真实 AI 应用系统，并能讲清楚技术实现、应用价值、创新点和团队协作。

推荐展示方式：

- 现场运行系统，演示完整业务流程。
- 打开关键代码，讲清楚核心逻辑。
- 做 2-3 个可控技能操作，例如调整置信度阈值、触发 AI 建议、查看预警记录。
- 每名学生只讲自己负责模块的关键技术，不平均背诵所有代码。

不推荐现场操作：

- 现场训练模型。
- 现场新增接口。
- 现场修改数据库结构。
- 现场安装依赖。
- 现场临时解决环境报错。

## 2. 四人分工总览

| 学生  | 角色             | 核心任务                           | 建议时长     |
| --- | -------------- | ------------------------------ | -------- |
| 学生一 | 项目经理 / 总控答辩负责人 | 背景、目标、架构、创新点、总结答辩              | 10-12 分钟 |
| 学生二 | 前端负责人          | 页面演示、用户流程、实时数据展示、图表和报告         | 10-12 分钟 |
| 学生三 | 后端负责人          | 接口、数据库、记录管理、AI 建议兜底            | 9-10 分钟  |
| 学生四 | 算法负责人          | YOLOv8、OpenCV、Socket 推送、时序风险判断 | 10-12 分钟 |

所有成员都必须掌握：

- 项目解决什么问题。
- 前端、后端、算法、数据库如何协同。
- 比赛现场完整演示路径。
- 自己模块的关键代码和输入输出。
- 

## 3. 50 分钟逐分钟流程

### 0-3 分钟：项目经理开场

目标：快速让评委知道项目做什么、为什么有价值。

建议话术：

> 各位评委老师好，我们团队展示的项目是课堂行为检测与 AI 辅助干预系统。传统课堂监控更多用于事后回看，缺少对课堂异常状态的主动发现和后续干预支持。我们的系统通过计算机视觉识别课堂中的趴桌、持续低头、疑似玩手机、疑似睡觉等行为，并结合时间维度生成风险事件，最终为教师提供温和、非诊断式的沟通建议和干预报告。

必须讲到：

- 不是为了“监控学生”，而是辅助教师发现课堂异常状态。
- 输出结果不做医学或心理诊断，只作为教师沟通参考。
- 系统重点是从识别到干预的闭环。

### 3-8 分钟：项目经理讲架构和流程

展示内容：

- Vue 前端：负责上传、检测展示、统计图表、AI 建议、报告。
- Spring Boot 后端：负责接口、文件、记录、数据库、AI 建议接口。
- Flask 算法服务：负责 YOLOv8 推理、视频帧处理、Socket 实时推送。
- MySQL 数据库：保存用户、检测记录、预警事件。

核心流程：

```text
视频/摄像头输入
  -> Flask 调用 YOLOv8 逐帧检测
  -> 统一行为标签和置信度
  -> 统计连续时长和累计时长
  -> 达到阈值生成风险事件
  -> Socket.IO 推送给前端
  -> Spring Boot 保存预警记录
  -> 前端展示统计、AI 建议和报告
```

建议打开的文件：

- `DETECTION_PROTOCOL.md`：说明前端、后端、算法之间的数据协议。
- `PROJECT_DOCUMENTATION.md`：说明整体项目架构。

### 8-20 分钟：前端负责人演示

目标：让评委看到系统是一个可用的应用，而不是单纯算法脚本。

演示顺序：

1. 进入视频检测页面。
2. 展示视频素材区域、模型选择、置信度滑块。
3. 点击开始检测或使用演示数据说明检测过程。
4. 展示右侧 AI 辅助干预区域。
5. 切换到检测结果页面，查看事件列表。
6. 切换到行为统计页面，讲图表含义。
7. 切换到 AI 辅助干预页面，生成沟通建议。
8. 切换到干预报告页面，展示报告导出。

重点技术：

- Vue3 + Vite：前端应用框架和构建工具。
- Element Plus：按钮、表格、标签、上传组件。
- ECharts：行为统计图表。
- Axios：请求 Spring Boot 后端接口。
- Socket.IO 客户端：接收 Flask 实时检测数据。
- `demoState`：统一维护事件、统计、AI 建议、报告状态。

关键代码讲解：

- `classDetectionVue/src/views/videoPredict/index.vue`
  - 视频上传。
  - 模型选择。
  - 置信度阈值。
  - 检测状态和图表展示。
  - Socket 数据接收后的页面更新。
- `classDetectionVue/src/views/demo/demoState.ts`
  - 事件数据结构。
  - 行为统计数据。
  - 风险事件同步。
  - AI 建议和报告文本生成。
- `classDetectionVue/src/utils/socket.ts`
  - 前端如何连接算法服务。
  - 如何接收实时消息。

前端负责人建议话术：

> 前端不是简单展示检测框，而是按照教师使用流程设计。教师先上传课堂视频，系统检测后把原始识别结果转成事件列表、行为统计和风险等级，再进入 AI 辅助干预和报告页面。这样教师看到的不是一堆技术数据，而是可以直接理解和处理的课堂事件。

可控技能操作：

- 调整置信度滑块，并解释“阈值越高，系统越严格，误报会减少，但漏检可能增加”。
- 点击生成沟通建议，并说明“AI 建议是辅助教师沟通，不是诊断结论”。
- 切换统计图，说明行为占比如何帮助教师了解整段课堂状态。

### 20-30 分钟：后端负责人讲解

目标：证明系统不是静态页面，而是有真实数据流、接口和记录管理。

重点技术：

- Spring Boot：后端业务服务。
- MyBatis Plus：数据库访问。
- MySQL：检测记录和预警记录存储。
- RESTful API：前后端接口。
- 统一返回格式：前端可以稳定处理成功和失败结果。
- 跨域配置：保证 Vue、Spring Boot、Flask 三个服务协同。

关键业务链路：

```text
前端上传素材
  -> Spring Boot 保存文件并返回 URL
  -> 前端或后端调用 Flask 检测接口
  -> Flask 返回检测结果或推送预警事件
  -> Spring Boot 保存 warning_records
  -> 前端查询 /warningRecords/all 或分页接口
  -> 页面展示事件和报告
```

关键代码讲解：

- `classDetectionSpringboot/src/main/java/com/example/Kcsj/controller/PredictionController.java`
  - 接收图片检测请求。
  - 调用 Flask `/predictImg`。
  - 保存检测结果。
- `classDetectionSpringboot/src/main/java/com/example/Kcsj/controller/WarningRecordsController.java`
  - 查询预警记录。
  - 保存和更新预警事件。
  - `/warningRecords/advice` 生成 AI 干预建议。
  - 外部 AI 不可用时返回本地兜底建议。
- `classDetectionSpringboot/src/main/java/com/example/Kcsj/common/Result.java`
  - 统一返回 `code`、`msg`、`data`，方便前端判断。

后端负责人建议话术：

> 后端主要承担业务闭环。算法服务负责识别，但最终比赛项目不能只停留在识别结果上，所以我们把异常行为保存成预警事件，提供查询、更新、AI 建议生成和报告支撑。即使比赛现场外部 AI 接口不可用，系统也会返回本地兜底建议，保证展示稳定。

可控技能操作：

- 打开 `WarningRecordsController.java`，讲 `/advice` 接口。
- 展示数据库 `warning_records` 表或 SQL 文件。
- 说明 AI 接口失败时为什么仍能返回建议。

### 30-42 分钟：算法负责人讲解

目标：讲清楚 AI 核心能力，尤其是“时序预警”这个创新点。

重点技术：

- YOLOv8：目标检测模型。
- OpenCV：读取视频帧、摄像头帧、绘制检测结果。
- Flask：提供算法服务接口。
- Socket.IO：实时推送检测结果给前端。
- 标签归一化：把不同模型输出统一成标准行为标签。
- 时序统计：统计连续时长和累计时长。

算法流程：

```text
加载 class.pt 权重
  -> 读取视频或摄像头画面
  -> OpenCV 拆分为连续帧
  -> YOLOv8 对每帧推理
  -> 获取行为类别、置信度、检测框
  -> 统一行为标签
  -> 累计行为持续时间
  -> 超过阈值后生成风险事件
  -> Socket 推送到前端
```

行为类别：

- 举手互动：`raise_hand`
- 阅读：`read`
- 书写：`write`
- 疑似玩手机：`phone`
- 持续低头：`head_down`
- 趴桌：`lie_desk`
- 疑似睡觉：`sleep`
- 专注学习：`focus`

关键代码讲解：

- `classDetectionFlask/main.py`
  - 加载 YOLO 模型。
  - 处理图片、视频、摄像头。
  - 使用 Socket.IO 推送实时结果。
  - `BehaviorWarningTracker` 进行行为持续时间判断。
- `classDetectionFlask/detection_protocol.py`
  - 统一协议字段。
  - 行为标签归一化。
  - 生成 `behaviorStats` 和 `warningState`。
- `DETECTION_PROTOCOL.md`
  - 解释推送给前端的数据结构。

算法负责人建议话术：

> 单帧检测容易受到姿态、遮挡和角度影响，所以我们没有直接把某一帧的结果当作最终风险，而是引入连续时长和累计时长。比如趴桌或疑似睡觉持续超过设定阈值后，才会生成高风险事件。这样可以降低偶然动作造成的误报。

可控技能操作：

- 选择 `class.pt` 权重。
- 展示置信度和检测框。
- 讲一条 Socket 数据如何变成前端风险事件。
- 说明阈值可以根据学校场景配置。

### 42-47 分钟：项目经理总结创新点

建议总结为 4 点：

1. 从单帧识别到时序行为分析。
2. 从检测结果到教师干预建议。
3. 从事后查看到主动预警。
4. 从单一算法脚本到前后端、算法、数据库协同系统。

建议话术：

> 本项目的重点不是只识别学生动作，而是把识别结果转化为教师可理解、可追踪、可处理的课堂事件。系统通过行为时长统计降低单帧误判，通过匿名化事件和本地处理降低隐私风险，通过 AI 辅助建议帮助教师进行温和沟通，形成了从检测到干预的完整闭环。

### 47-50 分钟：预留答辩和应急

用于：

- 评委临时提问。
- 系统卡顿时补充代码讲解。
- 展示数据库或关键接口。
- 项目经理做收尾。

## 4. 每名学生必须掌握的代码范围

### 全员必须掌握 30%

所有人都要能讲：

- 项目名称和定位。
- 为什么做这个项目。
- 系统完整流程。
- 四个模块如何配合。
- 创新点和应用价值。
- AI 建议不是心理诊断。

### 自己模块必须掌握 80%

每个人必须能讲清：

- 自己模块的技术栈。
- 自己模块的关键文件。
- 输入是什么。
- 输出是什么。
- 异常情况如何处理。
- 和其他模块如何对接。

### 展示代码必须掌握 100%

比赛现场准备打开讲解的代码必须逐段理解。不能出现“这段代码是模板，我也不太清楚”的情况。

建议每人只准备 2-3 个关键文件，不要贪多。

## 5. 答辩问题标准答案

### 为什么使用 YOLOv8？

参考回答：

> YOLOv8 推理速度快，适合视频帧和摄像头画面的实时检测；同时模型部署相对简单，便于和 Flask 服务结合。本项目重点是课堂行为场景，所以我们使用训练好的课堂行为权重进行识别，再结合时序统计生成风险事件。

### 如何降低误判？

参考回答：

> 我们没有直接把单帧识别结果作为最终预警，而是结合连续时长和累计时长。例如短暂低头可能只是看书或写字，只有持续超过阈值后才生成风险事件。同时前端提供置信度阈值配置，可以根据实际场景调整检测严格程度。

### 是否侵犯学生隐私？

参考回答：

> 项目设计时避免绑定学生实名信息，主要以课堂事件为单位展示结果。系统定位是辅助教师观察课堂状态，不做公开点名和心理诊断。后续实际应用中可以采用本地部署、边缘计算、权限控制和数据脱敏来进一步保护隐私。

### AI 建议是否等于心理诊断？

参考回答：

> 不是。AI 建议只提供教师沟通参考，内容要求温和、非诊断、不贴标签。系统不会判断学生心理疾病，也不会替代教师、心理老师或专业人员的判断。

### 如果算法服务断开怎么办？

参考回答：

> 系统采用前后端和算法服务解耦设计。算法服务异常时，前端仍可以展示已有检测记录和演示兜底数据；AI 建议接口也有本地兜底内容，保证比赛现场和基本业务流程稳定。

### 数据库保存哪些内容？

参考回答：

> 数据库主要保存用户信息、图片检测记录、视频检测记录、摄像头记录和预警记录。预警记录包括行为类型、风险等级、持续时长、触发原因、处理状态和建议内容，用于后续查询和报告生成。

### 前端如何收到实时检测结果？

参考回答：

> Flask 算法服务在处理视频帧时，通过 Socket.IO 推送标准协议数据。前端 Socket 客户端接收后，更新 `demoState` 中的事件列表、统计数据和当前风险状态，页面会随状态变化自动刷新。

### 为什么还需要 Spring Boot，不能前端直接调 Flask？

参考回答：

> Flask 更适合承载算法推理，Spring Boot 更适合承载业务管理。比如文件管理、数据库记录、用户管理、预警查询、AI 建议接口都放在后端业务服务里更规范，也便于后期权限控制和系统扩展。

### 这个项目的创新点是什么？

参考回答：

> 第一，从单帧检测升级为连续时长和累计时长分析。第二，从行为识别升级为风险事件和干预建议。第三，采用前端、后端、算法服务解耦，后续可以替换模型或扩展场景。第四，强调隐私保护和非诊断式辅助，符合教育场景使用要求。

### 如果评委要求现场写代码怎么办？

参考回答：

> 可以选择做低风险、小范围操作，例如调整行为阈值或置信度配置，并说明该配置如何影响检测结果。不建议现场新增复杂功能，因为展示重点是系统完整性和稳定性。

## 6. 彩排安排

### 第一轮：讲通流程

目标：四个人知道谁接谁，避免重复和断档。

检查项：

- 项目经理能在 3 分钟内讲清背景。
- 前端能完整演示页面。
- 后端能讲清接口和数据库。
- 算法能讲清 YOLO 和时序预警。

### 第二轮：严格计时

目标：控制在 47 分钟内，留 3 分钟应急。

建议时间：

- 项目经理开场和架构：8 分钟。
- 前端：12 分钟。
- 后端：10 分钟。
- 算法：12 分钟。
- 项目经理总结：5 分钟。

### 第三轮：模拟答辩

目标：训练被打断后的应答能力。

做法：

- 老师或同学随机提问。
- 每人至少回答 5 个问题。
- 回答控制在 30-60 秒。
- 不会的问题不要硬编，转为“当前版本如何处理、后续如何扩展”。

## 7. 现场应急方案

### 前端页面打不开

处理方式：

- 项目经理先讲架构和创新点。
- 前端负责人打开关键代码和截图说明。
- 后端或算法负责人补充接口和数据协议。

### 算法检测速度慢

处理方式：

- 使用短视频素材。
- 降低视频分辨率。
- 使用已有演示数据说明检测结果。
- 强调比赛展示重点是完整流程，不是长时间跑完整视频。

### AI 接口不可用

处理方式：

- 后端负责人说明系统有本地兜底建议。
- 演示 `/warningRecords/advice` 返回 fallback 建议。
- 强调这是为了保证教育场景下系统稳定可用。

### 数据库连接异常

处理方式：

- 前端使用演示兜底数据继续展示。
- 后端负责人打开实体类、Controller 和 SQL 文件说明设计。
- 不在现场临时修数据库环境。

## 8. 每人准备材料清单

### 项目经理

- 1 张系统架构图。
- 1 张业务流程图。
- 4 个创新点。
- 5 个常见问题答案。
- 30 秒、2 分钟、5 分钟总结稿。

### 前端负责人

- 页面演示顺序。
- 3 个关键文件。
- 置信度阈值操作说明。
- Socket 数据更新页面的说明。
- 页面卡顿时的代码讲解方案。

### 后端负责人

- 接口列表。
- 数据库表说明。
- `PredictionController` 讲解稿。
- `WarningRecordsController` 讲解稿。
- AI 建议兜底说明。

### 算法负责人

- YOLOv8 选择理由。
- 行为类别表。
- 视频帧处理流程。
- 时序阈值说明。
- Socket 推送协议说明。

## 9. 最终展示定位

本项目展示的核心不是“我们写了多少代码”，而是：

- 能识别课堂行为。
- 能把识别结果转成风险事件。
- 能把风险事件转成教师可执行建议。
- 能用前后端、算法、数据库协同完成真实应用闭环。
- 能体现职业素养、隐私意识和团队协作能力。

四名学生的最佳状态是：每个人都像自己模块的小负责人，而不是背稿人。项目经理讲价值，前端讲体验，后端讲工程，算法讲智能，四条线合起来就是一个完整的人工智能应用项目。
## 现场讲解补充：行为统计与图表口径

在演示 `#/videoPredict` 的“行为统计”区域时，统一使用以下解释，避免把算法明细误讲成普通视频时长。

### 统计字段含义

- `累计人次时长`：按学生目标轨迹累计的行为时长，单位可以理解为“人次秒”。例如 3 个学生同时低头 10 秒，系统统计为 30 人次秒。因此累计人次时长可能大于视频本身播放时长，这是多目标课堂场景的正常结果。
- `累计占比`：某一行为的累计人次时长 / 所有行为累计人次时长。它反映整段素材中不同课堂状态的相对占比，不是视频时间轴上单一行为的占比。
- `片段`：同一学生目标在同一行为下，经过合并后的连续行为片段数量。它不是视频被切成了多少段，也不是右侧 AI 预警事件数。
- `置信度`：该行为类别在 CSV 明细中的平均识别置信度分档，用于辅助判断统计可信度，不单独作为干预结论。

### 图表含义

- `人次时长趋势图`：横轴是视频时间，纵轴是每个时间桶内该行为的累计人次秒。曲线高低表示该时间段内同时出现该行为的学生目标数量和持续时间综合结果。
- `累计行为占比图`：展示整段视频中各行为累计人次时长的比例。它与统计卡片的 `累计占比` 使用同一口径。
- 两张图的颜色按行为固定映射，趋势图和环图中同一颜色代表同一行为，便于评委对照查看。

### 与 `predictions.csv` 的关系

`predictions.csv` 是算法输出的逐目标滑窗预测明细，每一行通常包含 `track_id`、`start_frame`、`end_frame`、`pred_label`、`confidence` 等字段。由于 LSTM/BiLSTM 会用重叠时间窗预测行为，同一学生同一行为会产生多行重叠记录。

前端展示前，Flask 会先按 `trackId + behaviorType` 合并重叠窗口，再生成：

- `behaviorStats`：统计卡片和累计行为占比图使用的数据。
- `predictionWindows`：人次时长趋势图使用的数据。

因此页面统计不是随意模拟数据，而是由 `class/output/<素材编号>/predictions.csv` 解析、合并和聚合得到。现场如果被问到“为什么片段数有几百个”，可以回答：这是多个学生目标在整段视频中被跟踪到的连续行为片段总数，课堂视频里目标多、轨迹有遮挡和重新编号，所以片段数会明显大于预警事件数。

### 推荐讲解话术

> 这里的统计不是单纯的视频秒数，而是“人次时长”。如果同一时间有多个学生出现同类行为，会分别累计，这样更符合课堂群体状态分析。算法输出的 `predictions.csv` 是逐目标、重叠滑窗的预测明细，系统会先合并同一学生同一行为的重叠窗口，再计算累计时长、片段数、趋势图和占比图。右侧 AI 干预只针对达到阈值的风险事件，左侧统计则反映整段课堂素材的总体状态。
